{
	"name": "purgefs",
	"version": "1.0.0",
	"description": "Command line utility to recursively delete all files and folders that match a regex.",
	"author": "Thiago Pereira",
	"keywords": ["fs", "purge", "delete", "recursively", "utility"],
	"scripts": {
		"test": "mocha ./tests --recursive --reporter spec --exit"
	},
	"bin": {
		"purgefs": "./index.js"
	},
	"dependencies": {
		"commander": "^2.14.1"
	},
	"devDependencies": {
		"chai": "^4.1.2",
		"mocha": "^5.0.1",
		"mock-fs": "^4.4.2"
	}
}
